REACT, FIREBASE, GOOGLE VISION App

- Git clone project
  -make sure you have `node`, `yarn`, `npm` installed

- In project directory run `npm install`

- After all dependecies are installed: run `yarn start` to start project on your loalhost.
